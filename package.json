{
  "name": "ejercicio-wizard",
  "version": "1.0.0",
  "license": "MIT",
  "source": "./src/index.html",
  "scripts": {
    "dev": "parcel",
    "build": "parcel build --dist-dir build",
    "deploy": "gh-pages -d build"
  },
  "devDependencies": {
    "gh-pages": "^6.1.1",
    "parcel": "^2.11.0"
  }
}
